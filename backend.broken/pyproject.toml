[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
# Reasonable defaults: pycodestyle, pyflakes, isort, bugbear
select = ["E", "F", "I", "B", "UP"]
ignore = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]